# Save4 — 桌宠形象修改指南

> 先说结论：**当前桌宠不是图片素材**，而是写死在 `index.html` 里的手绘 SVG 矢量猫，
> 颜色由 CSS 变量控制，动画由 CSS keyframes 驱动。
> 所以“换形象/改动态”都是在代码里改，不需要（也不存在）图片素材文件。

---

## 一、当前素材从哪来？（文件清单）

| 素材 | 用途 | 在哪里定义 |
|---|---|---|
| 🐱 桌宠本体 | 手绘 SVG 几何猫 | `index.html` 里的 `<svg id="pet-svg">`（约 40 行矢量图形） |
| 🎨 毛色/描边/腮红 | CSS 变量 | `css/style.css` 顶部 `:root` |
| 😺 三种表情 | SVG 里的三组 `<g>` | `index.html` 的 `.expr-idle/.expr-happy/.expr-angry` |
| ✨ 动画 | CSS keyframes | `css/style.css`（blink 眨眼 / idle-bob 浮动 / fx-jump 跳起 / fx-shake 炸毛） |
| 表情切换逻辑 | pet.js | `js/pet.js`（`setExpression` / `fx` / `celebrate`） |
| 🖼 flipped_cut.png | 动漫小人皮肤（立绘） | `index.html` 的 `<img id="pet-img">` |
| 🖼 liuge.png | 刘哥皮肤（真人照片素材，**未随仓库发布**） | `index.html` 的 `<img id="pet-img-liuge">`，由 `scripts/cut-liuge.js` 抠图生成 |
| 🎯 shortcut_icon.ico | 桌面版托盘/通知图标 | `electron/main.js`（`ICON_PATH`） |

---

## 二、最常用的修改

### 1）只想换毛色（10 秒搞定）

打开 `css/style.css` 顶部：

```css
--pet-fill: #f9c877;   /* 身体/头/耳朵主色（橙） */
--pet-line: #5c4132;   /* 描边色（深棕） */
--pet-inner: #f58f7b;  /* 耳内粉 */
```

例如改成灰猫：`--pet-fill: #b8bcc4; --pet-line: #4a4d55;`
改成黑猫：`--pet-fill: #4a4d55; --pet-line: #2b2d33;`

### 2）换表情的“长相”（眼睛/嘴的弧度）

在 `index.html` 的 `<svg>` 里，三组表情分别画眼睛嘴巴：
- `.expr-idle`（日常圆眼 + w 嘴）
- `.expr-happy`（^ ^ 笑眼 + 张嘴）
- `.expr-angry`（凶眼 + 皱眉）

眼睛的坐标风格示例（idle 圆眼）：
```html
<ellipse class="eye" cx="47" cy="53" rx="4.2" ry="6"/>
```
想改瞪圆眼睛 → 调 `rx/ry`；想改豆豆眼 → 改 `cx/cy` 与尺寸；
想加新表情（比如困倦 😪）→ 复制一组 `<g class="expr expr-zzz">`，
再到 `js/pet.js` 的表达式名单加一项。

### 3）调节奏：眨眼频率 / 浮动幅度

`css/style.css`：
- `blink 4.6s infinite` → 眨眼间隔（改大 = 眨得慢）
- `idle-bob` 的 `translateY(-6px)` → 呼吸浮动幅度

---

## 三、换一整套形象（猫 → 狗/像素/二次元）

有两条路：

### 路线 A：保持“SVG 矢量 + 表情组”机制（推荐，动画全保留）

只替换 `index.html` 里 `<svg id="pet-svg">` 的**内部图形**，约束：
1. 保留 `viewBox="0 0 120 120"`
2. 保留三组 `.expr-*`（或至少 idle），形状按你的新形象画
3. 想复用“眨眼”CSS 就给眼睛元素加 `class="eye"`，想复用“开心眯眼”就用同类路径
4. 表亲颜色继续走 CSS 变量（`--pet-fill` 等）最省事

优点：拖拽/缩放（SVG 无损）、表情切换、跳起/炸毛动画全部自动继续工作。

### 路线 B：直接用图片/像素素材（png / gif / 精灵图）

适合你已有的像素画或立绘素材：

1. 把图片放进项目，例如 `assets/pet.png`
2. 用 `<img>` 替换（或覆盖）`#pet-svg`，或另起一个 `#pet-img` 层
3. 动画需适配：
   - 跳起/炸毛：给图片容器加同样的 `fx-jump / fx-shake` keyframes 即可（CSS 动画作用于整个元素，与是不是图片无关）
   - **表情切换/眨眼需要“多帧素材”**：常见做法是
     ① 整图替换：`setExpression` 时换 `img.src`（需要 idle/happy/angry 三张图）
     ② 精灵图：一张雪碧图 + `background-position` 切帧（性能更好，但要做切图）
     ③ 动态 gif：本身就带动画，但无法和“提醒时才炸毛”联动

> 若你做像素风，建议一套像素图 3 张（idle / happy / angry），
> 走路线 B①，最省事且和现有 `pet.setExpression('happy')` 调用天然兼容。

---

## 四、动画效果“谁触发谁”

| 效果 | 触发场景 | 代码 |
|---|---|---|
| 眨眼睛（自动） | 常驻 idle | CSS `blink` 动画 |
| 呼吸浮动（自动） | 常驻 | CSS `idle-bob` |
| 眯眼笑 + 小跳 | 拖动松手 / 待办提醒 | `pet.celebrate()` → 临时切 happy + `fx('happy')` |
| 炸毛抖动 + 生气脸 | 定时器到点（强提醒） | `pet.fx('angry')` + `setExpression('angry')` |
| 气泡弹出 | 待办到点 / 定时到点 | 气泡是独立元素（`#bubble`），在桌宠上方 |

想“新增一种动画情绪”，步骤：
1. `style.css` 加 keyframes + 一个 `.fx-新名字` 规则
2. `pet.js` 的 `fx()` 里把新类名加进清理列表
3. 在需要的地方调 `Save4.pet.fx('新名字')`

---

## 五、参考：现在这只猫是怎么画出来的

几何小猫的构成（全部是基本形状拼的）：
- 两个三角形 → 耳朵（`.ear`），两个小三角 → 耳内（`.ear-inner`）
- 一个圆 → 头（`.head`），一个椭圆 → 身体（`.body`）
- 几段短直线 → 胡须（`.whiskers`），两个半透明圆 → 腮红（`.blush`）
- 表情组内：椭圆=眼 / path=嘴 / path=眉毛

所有形状共用 `fill: var(--pet-fill)`、`stroke: var(--pet-line)`，
所以改两个 CSS 变量就能整体换色——这就是“几何风格换肤”的核心技巧。
