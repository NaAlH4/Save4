# 桌面贪吃蛇 — 实现方案与难度分析

> 目标：右键菜单开启游戏；桌宠变成蛇头，桌面图标变成豆；方向键/WASD 控制；
> 吃完所有图标胜利；撞身体或屏幕边缘失败；ESC 退出；**不影响桌面上任何文件与图标位置**。

---

## 一、结论先说

| 方案 | 说明 | 难度 | 风险 |
|---|---|---|---|
| **A. 覆盖层「虚拟桌面」（推荐）** | 游戏跑在现有 Electron 透明覆盖层里；用**真实桌面文件的图标**当豆；用遮罩让真实图标"消失" | ★★★☆☆（中等） | **零风险**：从不碰真实图标，退出即恢复 |
| **B. 操作真实 Windows 桌面图标** | 真的隐藏/移动 `SysListView32` 里的桌面图标 | ★★★★★（高） | 高：需 Win32/COM 原生调用、权限受限、自动排列冲突、**崩溃后桌面残留被改乱** |

**推荐 A。** 关键洞察：**不需要碰真实图标，也能满足"所有图标变成豆"**——因为 Electron 能拿到桌面文件的**真实图标图像**：

- `app.getPath('desktop')` + `fs.readdir` → 拿到真实条目（含公共桌面 `C:\Users\Public\Desktop`）
- `app.getFileIcon(path, { size: 'normal' })` → 拿到该文件的**真实图标**（`Promise<NativeImage>`）
- `NativeImage.toDataURL()` → 传给渲染层当"豆"显示

> 已在本机核实：Electron 44.3.0 支持上述三个 API；当前桌面共 **38 个**条目（用户 25 + 公共 13），
> 即一局约 38 颗豆，节奏合理。

**"退出后一切恢复原有"因此天然成立**——因为我们从头到尾没有修改任何文件或图标位置。
游戏开始时只是用一层半透明遮罩盖住桌面（视觉上图标消失），退出时移除遮罩即可。

---

## 二、技术设计

### 2.1 坐标与网格

```
工作区 workArea(W×H)  →  cell = 40px（可调）
cols = floor(W / cell)   rows = floor(H / cell)
整块网格居中，四周留边
```

- 蛇的位置用**格子坐标** `{cx, cy}` 记录
- 蛇身用队列保存历史格子（`segments[]`），尾部随长度增长而保留
- 屏幕边缘 = 撞网格边界（失败）

### 2.2 渲染（用 DOM，不用 Canvas）

| 元素 | 实现 |
|---|---|
| 蛇头 | `#game-head`：`background-image` 取**当前皮肤**的图片（anime / 自定义 / flipped_cut.png）；几何小猫皮肤时退化为内联小猫图形 |
| 蛇身 | 每格一个 `.seg` 圆角块（用皮肤主色） |
| 豆 | 单个 `.food` 元素，内含真实图标 `<img src="data:...">` |
| 背景遮罩 | 全屏 `rgba(0,0,0,.55)` + `backdrop-filter: blur(3px)`，让桌面变成"游戏面板" |
| HUD | 左上角：分数 / 剩余 / 用时；右上角：退出按钮 |

> 为什么不用 Canvas：格子数量少（约 47×26），DOM 完全够用；且能**直接复用现有皮肤图片与真实图标 `img`**，动画/样式改造最快。

**移动平滑**：蛇头用 `transform: translate()` + CSS `transition` 做插值，视觉上是"滑过去"而不是跳格。

### 2.3 游戏规则（严格对齐需求）

| 规则 | 实现 |
|---|---|
| 同一时间**只有一颗豆** | 只渲染一个 `.food` |
| 吃掉后**随机位置刷新下一颗** | 从剩余图标池取下一个，随机选空格（不与蛇身/当前豆重合） |
| **吃完所有图标 → 胜利** | 图标池耗尽且最后一颗被吃 → 结算「通关」 |
| 撞身体 / 撞边缘 → 失败 | 撞边界或 `segments` 命中头部下一格 |
| 计分 | 每吃一颗 +1（或按图标类型加权，可选） |
| 桌面无图标 | 兜底：用内置图标生成 10 颗豆，保证可玩 |

### 2.4 控制与输入

| 按键 | 作用 |
|---|---|
| ↑↓←→ / WASD | 转向（**禁止 180° 反向**，用输入缓冲队列避免快速连按丢键） |
| 空格 | 暂停 / 继续 |
| ESC | 退出游戏，恢复原状 |

**焦点问题（本方案最主要的坑）**：覆盖层默认是「点击穿透」的，键盘事件不会进来。因此游戏开始时必须：

1. 渲染层进入"强制可交互"状态（`desktop.js` 增加 `forceInteractive` 开关）
2. 主进程 `win.focus()` 抢焦点
3. 监听 `blur` → **自动暂停**（防止用户点了别的窗口后蛇还在跑）
4. 方向键 `preventDefault()`，避免页面滚动

### 2.5 进入 / 退出流程

```
右键菜单「🎮 开始游戏」
  ├─ 记录快照：mode、petGeo(位置/尺寸)、皮肤、面板/对话是否打开
  ├─ 预取桌面图标（异步，显示"加载中…"）
  ├─ 隐藏桌宠本体/面板/对话/气泡；显示游戏层
  ├─ 抢焦点 + 关闭点击穿透
  └─ 开始 tick 循环
ESC / 结算面板「退出」
  ├─ 停止循环、移除游戏层
  ├─ 从快照还原：模式、桌宠位置尺寸、面板可见性
  ├─ 恢复点击穿透逻辑
  └─ 真实桌面：从未改动 → 天然原样
```

### 2.6 速度曲线

- 基础 tick `150ms`
- 每吃 5 颗 `-8ms`，下限 `70ms`
- 暂停/失焦时冻结计时

---

## 三、难度分解

| 子任务 | 难度 | 估时 | 说明 |
|---|---|---|---|
| 1. 游戏骨架：入口 + 覆盖层 + 网格 + 蛇移动 | ★★ | 1h | 纯逻辑，最直观 |
| 2. 输入处理（键盘/缓冲/防反向/焦点/失焦暂停） | ★★★ | 1h | **焦点与穿透互斥是最容易踩坑处** |
| 3. 碰撞与胜负判定、计分、结算面板 | ★★ | 0.5h | 规则简单 |
| 4. 蛇头复用桌宠皮肤 + 平滑移动 | ★★★ | 1h | 需处理几何猫（非图片）皮肤的退化 |
| 5. 真实图标：主进程枚举桌面 + `getFileIcon` + IPC | ★★ | 1h | API 已验证；注意批量耗时与缓存 |
| 6. 遮罩/虚化/HUD/音效/最高分 | ★★ | 1–2h | 打磨项 |
| 7. 完整恢复（模式/位置/穿透） | ★★★ | 0.5h | 必须逐一还原，避免"退出后桌宠卡住" |
| **合计（方案 A）** | **★★★☆☆** | **6–7h** | 分 3 期交付 |

**若坚持方案 B（真实操作桌面图标）**：额外 +3～5h，且需 PowerShell + `Add-Type` P/Invoke 调
`FindWindow("Progman") → FindWindowEx → SysListView32`，用 `LVM_GETITEMPOSITION/LVM_SETITEMPOSITION`
读写图标坐标。风险：
- 需与 Explorer 同权限级别（UIPI 可能拒绝）
- 「自动排列图标」开启时移动会被立刻弹回
- 进程异常退出 → **图标位置残留被改乱**（违背"恢复原有"的核心要求）
- 不同 Windows 版本/多显示器行为不一致

→ **建议不做**；若确实想要"真图标动起来"的效果，可考虑后期用 Win32 只做"隐藏/显示图标"
（`ShowWindow(SW_HIDE/SW_SHOW)`，不改坐标），风险仍高于收益。

---

## 四、分期实现计划

| 期 | 内容 | 交付标准 | 难度 |
|---|---|---|---|
| **P1 MVP** | 右键入口、游戏层、网格、蛇（头=皮肤图）、WASD/方向键、单豆（占位图标）、碰撞、计分、ESC 退出与完全恢复 | 能完整玩一局，退出后桌宠/面板原样 | ★★★ |
| **P2 真实图标** | 主进程枚举桌面 + `getFileIcon` → 豆用真实图标；吃完所有 → 通关 | 豆 = 你桌面上的真实文件图标，数量一致 | ★★ |
| **P3 打磨** | 速度递增、暂停、音效（WebAudio）、吃豆动画、HUD、最高分、结算面板、遮罩虚化 | 手感与观感完整 | ★★ |
| P4 可选 | 真·桌面图标隐藏或移动 | —— | ★★★★★（不推荐） |

---

## 五、已知限制（需提前知晓）

1. **仅覆盖主显示器**的工作区（现有覆盖层就是主屏）；副屏不参与游戏。
2. 游戏期间层会**捕获鼠标**（避免误点桌面），因此是模态体验——HUD 常驻「退出」按钮 + ESC 兜底。
3. 浏览器版（非 Electron）拿不到真实文件图标，豆会退化为内置图标（功能仍可玩）。
4. 若桌面图标数很少（例如 3 个），一局会很快结束；反之 38 个约 2–4 分钟。
5. 桌面文件名/图标属于隐私内容，**只在游戏内存中使用、不落盘、不外传**。

---

## 六、待确认的决策点

1. **采用方案 A（虚拟桌面，零风险）还是方案 B（真图标，高风险）？** → 建议 A
2. 豆用**真实桌面文件图标**，还是游戏内统一图标？ → 建议真实图标（更有"桌面"味）
3. 网格手感初值：`cell=40px`、基础 `150ms` 是否合适？（可先做出来再调）
4. 是否需要音效与最高分记录？
